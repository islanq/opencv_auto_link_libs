# opencv_auto_link_libs

Eliminates the need to manually add libs to compile your OpenCV projects.

- Include opencv_auto_link.hpp after your OpenCV includes.
- undef AUTO_LINK_STATIC_LIBS to disable.
